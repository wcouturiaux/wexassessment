info:
  name: Get All
  type: http
  seq: 1

http:
  method: GET
  url: "{{baseUrl}}/api/v1/transactions"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
